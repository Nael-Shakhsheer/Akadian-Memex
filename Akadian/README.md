# Memex Study release

Run `python3 -m http.server 8765 --directory memex` from this directory, then open http://localhost:8765.

- Study is the default entry point.
- Ideas is the original refinement workspace.
- Full guide: `memex/README.md`.
- Legacy Ideas guide: `memex/IDEAS.md`.
- Validation: `TESTING.md`.
