# Akadian · Memex

Memex is now a source-first AI study platform with the original idea-refining product available as **Ideas** mode.

Start in `Akadian/memex/README.md` for setup, features, privacy, implementation limits and the future model plan. Serve `Akadian/memex` over localhost or HTTPS. This archive contains source code, not a hosted deployment.
